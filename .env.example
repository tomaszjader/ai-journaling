# Przykładowe zmienne środowiskowe dla projektu
# Skopiuj ten plik jako `.env` i uzupełnij wartości.

# --- Frontend (Vite) ---
# Adres projektu Supabase
VITE_SUPABASE_URL="https://<twoj-projekt-ref>.supabase.co"

# Klucz publiczny (anon key) Supabase
VITE_SUPABASE_PUBLISHABLE_KEY="<twoj-anon-key>"

# Opcjonalnie: identyfikator projektu Supabase (jeśli używany)
VITE_SUPABASE_PROJECT_ID="<twoj-project-id>"

# --- Edge Functions (konfiguracja przez Supabase CLI, nie z pliku .env) ---
# Te sekrety ustaw przez: supabase functions secrets set
# Zostawione tutaj jako wskazówka.
# LOVABLE_API_KEY="<twoj-lovable-api-key>"
# AI_MODEL="google/gemini-2.5-flash"

# Uwaga:
# - Frontend korzysta z VITE_* podczas budowy/uruchomienia dev.
# - Funkcja `supabase/functions/chat` czyta sekrety z środowiska funkcji,
#   nie z pliku `.env` w repo. Ustaw je przez Supabase CLI.